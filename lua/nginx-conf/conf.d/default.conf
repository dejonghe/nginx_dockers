server {
    listen 80 default_server;
    server_name _;

    location / {
        root /var/www;
        content_by_lua '
            ngx.header["Content-Type"] = "text/plain";
            ngx.say("hello world");
        ';
    }
}
